'use strict';

describe('Service: LeadService', function () {

  // load the service's module
  beforeEach(module('cloudifyWidgetHpClientApp'));

  // instantiate service
  var LeadService;
  beforeEach(inject(function (_LeadService_) {
    LeadService = _LeadService_;
  }));

});
