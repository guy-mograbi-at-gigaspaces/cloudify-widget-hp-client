'use strict';

(function(exports) {

    // Define all your functions on the exports object
    exports.widgetServer = 'latestwidget.gsdev.info';

})(( typeof(process) === 'undefined' || !process.versions) ? window.conf = window.conf || {} : exports);